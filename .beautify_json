#!/usr/bin/env bash
if [ -z "$1" ]; then
  echo "Braucht ein Argument"
  exit
fi
filename=$1
pretty="pretty"
if [ -n "$2" ]; then
  pretty="$2"
fi
cat "$1" | python3 -m json.tool > "${filename%.*}_pretty_$pretty.json"
