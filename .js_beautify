#!/usr/bin/env bash
if [ -z "$1" ]; then
  echo "Braucht ein Argument"
  exit
fi
# js-beautify muss man installiert haben:
# npm -g install js-beautify
filename=$1
pretty="pretty"
if [ -n "$2" ]; then
  pretty="$2"
fi
js-beautify "$1" > "${filename%.*}_pretty_$pretty.js"
